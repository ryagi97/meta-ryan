#include "calc-func.h"

int add(int x, int y)
{
	printf("%d + %d = %d\n", x, y, x+y);
}

int sub(int x, int y) 
{
	printf("%d - %d = %d\n", x, y, x-y);
}

int mul(int x, int y)
{
	printf("%d * %d = %d\n", x, y, x*y);
}

int divide(int x, int y)
{
	printf("%d / %d = %d\n", x, y, x/y);
}

